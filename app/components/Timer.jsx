var React = require('react');

const Timer = React.createClass({
    render(){
        return(
            <div>
                <h1>Timer</h1>
            </div>
        )
    }
})

module.exports = Timer;