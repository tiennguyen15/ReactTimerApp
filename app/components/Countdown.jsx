var React = require('react');

const Countdown = React.createClass({
    render(){
        return (
            <div>
                <h1>Countdown</h1>
            </div>
        )
    }
})

module.exports = Countdown;